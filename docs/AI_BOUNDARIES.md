# AI Boundaries

AI is an interpreter and narrator, never the source of truth. Future providers must return validated proposals. They cannot directly change hit points, positions, inventory, relationships, quest state, or memories. The authoritative engine must accept or reject every proposed command.