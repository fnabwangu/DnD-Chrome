# Campaign Packs

Campaign content is data-only JSON. `pnpm validate:campaign` parses the manifest and world files and rejects executable-looking fields. Campaign packs do not get imported as JavaScript.