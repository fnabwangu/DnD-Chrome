# Infrastructure & Deployment

This directory contains container configurations and deployment manifests for future environments.

For local development:
- Node.js >= 20
- pnpm >= 10
- Pure in-memory adapters (no external database or API keys required).
